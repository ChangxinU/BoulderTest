任务：
基于仓库中的实际攀岩数据集，实现数据集适配层。

目标：
让训练和评估模块能够读取 `ml/data/` 中的真实数据。

请完成：
1. 检查数据布局和标注格式
2. 更新 `docs/dataset_spec.md`
3. 在 `ml/datasets/` 中实现对应的数据适配器
4. 如有必要，在 `ml/tools/` 中新增格式转换工具
5. 提供一个最小的数据加载验证方法

约束：
- 优先支持真实发现的数据格式
- 不要假设不存在的字段
- 如格式不统一，请记录清洗或转换策略
- 保持代码清晰、模块化

输出要求：
- 修改文件列表
- 关键数据假设
- 如何运行验证
- 风险说明
